// Váriaveis declaradas e alterações das várias já declaradas.
let nome = "Super man";
let idade = 100;
let poder = "Super força";

nome = "Batman";
poder = "Rico";

// Chamando as váriaveis
console.log(
  `Nome: ${nome}, 
 Idade: ${idade}, 
 Poder: ${poder}`
);

// OBS: não utilizar mais a variavel com var, pois o comum é let para variaveis que se alteram e const para aquelas que não irão alterar
